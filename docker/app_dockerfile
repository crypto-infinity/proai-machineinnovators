# Dockerfile per FastAPI backend
FROM python:3.12

WORKDIR /

COPY requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY / .
EXPOSE 8000

ENTRYPOINT ["gunicorn", "api.backend:app"]